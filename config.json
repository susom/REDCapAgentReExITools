{
	"name": "REDCapAgentRexiTools",

	"namespace": "Stanford\\REDCapAgentRexiTools",

	"description": "REDCapAgentRexiTools is a lightweight External Module scaffold for RExI-specific agent tools used by the SecureChatAI agent orchestration system.",

	"documentation": "README.md",

	"authors": [
		{
			"name": "Irvin Szeto",
			"email": "irvins@stanford.edu",
			"institution": "Stanford University"
		}
	],

	"permissions": [

	],
	
	"framework-version": 14,
    "enable-every-page-hooks-on-system-pages": false,

	"links": {
		"project": [

		],
		"control-center": [

		]
	},

	"api-actions": {
		"escalation_create": {
			"description": "Agent tool: create an escalation ticket in the configured escalation project",
			"access": ["auth"]
		},
		"escalation_list": {
			"description": "Agent tool: list escalation tickets for the current user with optional status filter",
			"access": ["auth"]
		},
		"escalation_get": {
			"description": "Agent tool: fetch details for a specific escalation ticket by record_id",
			"access": ["auth"]
		}
	},

	"agent-tool-definitions": [
		{
			"name": "escalation.create",
			"description": "Create an escalation ticket for a research project issue",
			"api-action": "escalation_create",
			"parameters": {
				"type": "object",
				"properties": {
					"subject": {"type": "string", "description": "Brief subject line for the escalation"},
					"summary": {"type": "string", "description": "Detailed description of the issue"},
					"priority": {"type": "string", "enum": ["low", "medium", "high"], "description": "Escalation priority level", "default": "medium"},
					"username": {"type": "string", "description": "REDCap username of the reporter"}
				},
				"required": ["subject", "summary"]
			},
			"readOnly": false,
			"destructive": false
		},
		{
			"name": "escalation.list",
			"description": "List escalation tickets for the current user, with optional status filter",
			"api-action": "escalation_list",
			"parameters": {
				"type": "object",
				"properties": {
					"status": {"type": "string", "enum": ["open", "in_progress", "resolved", "all"], "description": "Filter by ticket status", "default": "open"},
					"username": {"type": "string", "description": "REDCap username to filter by"}
				}
			},
			"readOnly": true,
			"destructive": false
		},
		{
			"name": "escalation.get",
			"description": "Fetch details for a specific escalation ticket by record ID",
			"api-action": "escalation_get",
			"parameters": {
				"type": "object",
				"properties": {
					"record_id": {"type": "string", "description": "The escalation ticket record ID to fetch"}
				},
				"required": ["record_id"]
			},
			"readOnly": true,
			"destructive": false
		}
	],

	"project-settings": [
		{
			"key": "enable-project-debug-logging",
			"name": "<b>Enable Debug Logging</b><br><i>(optional)</i> Requires installation and configuration of emLogger",
			"required": false,
			"type": "checkbox"
		}
	],

	"system-settings": [
		{
			"key": "escalation-project-id",
			"name": "<b>Escalation Project ID</b><br><i>Project that stores escalation tickets</i>",
			"required": false,
			"type": "project-id"
		},
		{
			"key": "enable-system-debug-logging",
			"name": "<b>Enable Debug Logging (system-wide)</b><i>(optional)</i> Requires installation and configuration of emLogger",
			"required": false,
			"type": "checkbox"
		}
	],

	"compatibility": {
		"php-version-min": "",
		"php-version-max": "",
		"redcap-version-min": "",
		"redcap-version-max": ""
	}
}
